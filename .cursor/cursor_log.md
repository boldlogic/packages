## 2026-04-27

### Сессия: правила «док не истина», projectlint, лог

**Проблема (зафиксировано сразу):** номер релиза в README ранее поднимали, опираясь только на старую строку в файле (`v0.1.6` → `v0.1.7`), без `git fetch --tags` и сверки с удалёнными тегами; на `origin` уже были `v0.1.7`…`v0.1.9`, поэтому `v0.1.7` в доке выглядело как откат и ломало доверие к процессу.

**Сделано:** в `.agents/rules/project-rules-cursor.mdc` явно зафиксировано: документация не источник истины; при рассинхроне док ↔ git/код — сразу блок «Проблема» в этом логе; в разделе релизов — обновление строки «Текущий релиз» только после сверки с тегами. В `cmd/projectlint` добавлена проверка README vs максимальный локальный тег `v*.*.*` с **stderr** с человеческим объяснением при отставании README; переменная `PROJECTLINT_SKIP_README_RELEASE` отключает проверку. В CI `actions/checkout` с `fetch-depth: 0` и `fetch-tags: true`. В `.project-rules.md` добавлен пункт 4.

**Как проверить:** `go run ./cmd/projectlint`; при README позади тегов — ненулевой код выхода и сообщение в stderr.

### Релиз v0.1.11 (только вперёд)

**Запрос:** новый релиз без правок истории старых коммитов (без amend/rebase).

**Сделано:** в README и `logger/zaplog/README` выставлен номер **v0.1.11**; в коммит включены проверка README↔git в `cmd/projectlint`, шаг CI с загрузкой тегов, журнал. Старые коммиты не переписывались.

**Итог:** после `git push` поставить аннотированный тег `v0.1.11` на этот коммит.
